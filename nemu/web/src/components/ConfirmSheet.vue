<script setup lang="ts">defineProps<{ open: boolean; title: string; description: string; action: string }>(); defineEmits<{ confirm: []; close: [] }>();</script>
<template><Transition name="fade"><div v-if="open" class="fixed inset-0 z-40 flex items-end bg-black/35 p-4 sm:items-center sm:justify-center" @click.self="$emit('close')"><section role="dialog" aria-modal="true" class="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl dark:bg-[#202721]"><div class="mx-auto mb-5 h-1 w-10 rounded-full bg-line sm:hidden" /><h2 class="text-lg font-semibold">{{ title }}</h2><p class="mt-2 text-sm leading-6 text-muted">{{ description }}</p><div class="mt-6 grid grid-cols-2 gap-3"><button class="soft-button" @click="$emit('close')">Batal</button><button class="inline-flex min-h-12 items-center justify-center rounded-2xl bg-red-600 px-4 text-sm font-semibold text-white active:scale-[.98]" @click="$emit('confirm')">{{ action }}</button></div></section></div></Transition></template>
