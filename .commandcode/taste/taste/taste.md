# Taste
- Wants the submit validation to be anchored to a reliable trigger rather than inferred from client-side DOM state: after a JS helper checking group/item attributes (surcas sudah ada) proved unreliable and still produced the blocking toast, prefers gating the "wajib" check by the flow type (only for new requests, skip for force/penyelesaian where the backend is authoritative and won't overwrite existing data) so the blocking toast can never appear in the completion path. Confidence: 0.6
- Prefers deployment verification to include actually exercising the previously-broken path on the VPS (or getting the user's confirmation) before declaring a bug fixed — reporting "sudah live di VPS" after commit c5985a8c alone was premature because the user still hit the same toast; a fix is only done when the user confirms the symptom is gone. Confidence: 0.7
- Prefers production service logs to be quiet and readable: routine info-level webhook/inbound logs ("biar tidak ramai sekali") should NOT appear — only errors and warnings should show by default; the assistant's applied pattern (gate info logs behind an opt-in env flag like `DEBUG_FONNTE=1` so detail can be re-enabled on demand, while `console.warn`/`console.error` always stay visible) matches this. Confidence: 0.85
- Communicates in Indonesian (Bahasa Indonesia) and expects assistant replies in Indonesian as well. Confidence: 0.9
- Communicates in a casual, informal tone — uses Indonesian slang ("apaan") and laughter ("wkwkwk") even when reporting bugs or pointing out that something is broken. Confidence: 0.8
- Tests UI changes hands-on in the browser after each change and reports back when the actual result doesn't match what the assistant described — including re-reporting the exact same symptom after a fix was deployed ("tetap saja muncul, 'Wajib pilih pengisi surcas'") until the behavior is genuinely resolved, not just syntactically deployed (e.g. a modal rendering as a plain unstyled form below the data instead of an overlay; a modal that appears on page load instead of only after clicking Edit). Confidence: 0.85
- Prefers larger, sharper, clearer UI text; finds default text too small and "too soft" — enlarge fonts and reduce softness/blur for readability. Confidence: 0.9
- Prefers concise, compact UI layouts: groups related data (e.g. primary + alternate phone numbers) into a single card with short letter badges (P for primary, S for secondary) instead of separate verbose sections — "ringkas" (concise). Repeatedly asks for multiple panels/cards (Customer Panel, Permintaan, Kurir, Auto Reply) to fit on a single row ("jadi kan satu baris") to save space — reducing action buttons to icon-only (no text label at all, "cukup icon saja, tanpa tulisan") so the icon can be made larger (e.g. `h-5 w-5` → `h-7 w-7`), with hover tooltip via `title` kept; also abbreviating labels (Auto Reply → "AR") and using a compact toggle switch for binary settings. Also dictates a precise vertical order for the panel (e.g. Nomor at top, then the Permintaan|Kurir|AR action row, then Kirim ke pelanggan) and expects the layout to follow that ordering exactly. Confidence: 0.9
- Wants UI colors to be harmonious/consistent (e.g. trash/delete icons in red to match other destructive elements). Confidence: 0.8
- Wants destructive actions (like delete) to always be preceded by a confirmation modal that is visually attractive/polished. Confidence: 0.9
- Prefers batch/bulk operations over one-by-one editing (e.g. download all prompts into one file to clean, then upload back to update everything at once). Confidence: 0.9
- Wants exported/downloaded markdown files to be cleaned up to be compact and uniformly structured, WITHOUT changing the content itself. Confidence: 0.8
- Values strict scope discipline: when asked to modify a specific file, change ONLY that file and nothing else. Confidence: 0.8
- Prefers AI prompt/intent rules written as explicit TRUE/FALSE conditions rather than negation words like "BUKAN"/"NOT"; clear, unambiguous, non-circular rules. Confidence: 0.8
- Dislikes intent prompts/content that redirect to other intents; each intent's prompt should stand alone without internal cross-references. Confidence: 0.7
- Wants 'Suggest (AI)' features to produce genuinely useful, working suggestions — not superficial tweaks like just appending a "NOT ..." phrase at the end. Confidence: 0.8
- Prefers keeping local per-developer tooling/config (e.g. `.commandcode/`) out of version control by adding it to `.gitignore`, instead of committing developer-local setup. Confidence: 0.8
- Treats sensitive runtime/session data (e.g. the WhatsApp `auth/` folder of fonnte_server) as something that must be gitignored and never committed — a non-gitignored auth folder is itself considered a problem ("apakah fonnte_server auth tidak di gitignore, sepertinya itu masalah"); expects this to be verified against actual git state (`git ls-files` / `git log --all -- <path>`) rather than assumed from `.gitignore` contents, and when the tracked-state check is clean, the real cause of VPS pull errors is diagnosed elsewhere (file ownership/permissions, not auth). Confidence: 0.6
- Wants dead/unused code (e.g. an unreferenced controller file, unneeded helpers/CSS/JS left over after a refactor) verified then deleted rather than kept around — values keeping the codebase clean of obsolete files. This extends to redundant UI: explicitly asks to remove cards/sections that became obsolete after their feature was merged elsewhere ("hilangkan" the old phone+copy card once copy moved to the new card). Confidence: 0.85
- When implementing a cross-cutting behavior (e.g. choosing which customer phone number to send WhatsApp notifications to), wants it applied uniformly across ALL customer-messaging paths (nota, selesai, deposit, member, etc.) — not scoped to just the initially-mentioned cases. Confidence: 0.9
- Prefers declining optional/out-of-scope feature suggestions (e.g. adding a new field to export/import) rather than expanding scope; once the core ask is covered, says "no need" to nice-to-haves. Confidence: 0.7
- Prefers editing a record via a single unified modal (an explicit "Edit" button opening one dialog containing all fields) rather than per-column inline/dblclick editing. Confidence: 0.9
- Wants UI components (modals, buttons, forms) to follow the project's documented UI Theme guidelines (e.g. `UI_THEME.md` design system): the `.op-modal` structure, single-hue gradient headers, sharp corners (no border-radius), uppercase labels, and theme token buttons. Confidence: 0.9
- Wants name-type fields (e.g. customer names) forced to UPPERCASE everywhere — both in the stored data and in the edit input — so values are always consistently capitalized. Confidence: 0.8
- When enforcing a data rule like UPPERCASE, wants it enforced at the backend save layer (not only client-side styling/JS), so stored data stays correct even if submitted via API/curl. Confidence: 0.7
- Wants validation errors and duplicate warnings for form edits displayed inline within the modal body (a styled inline alert above the fields), not only as a toast. Confidence: 0.8
- When enforcing uniqueness/data-integrity rules (e.g. duplicate phone numbers on edit), wants the check to cover all equivalent fields (both primary and alternate numbers) and reject the save against any other record in the same branch scope. Confidence: 0.7
- Wants form fields/UI to stay in sync with the actual database schema — a field with no backing column in the table (e.g. an `alamat` field absent from the `pelanggan` table) should be removed entirely from the edit flow (controller, view, JS) rather than kept around. Confidence: 0.6
- Prefers a staged submit flow for conflict-prone forms: the primary action (e.g. Save) starts disabled and only becomes enabled after a separate "Cek Dulu" (check-first) step runs; both a warning result and an OK result enable saving once the check has been performed. Confidence: 0.8
- Wants conflict/duplicate warnings to display the conflicting records' actual data (id, name, phone numbers), not just a generic "already used" message, so the user can judge whether the duplicate is intentional. Confidence: 0.7
- Distinguishes hard vs. soft uniqueness rules: fields the user declares strictly UNIQUE per scope (e.g. customer name per branch/cabang — "name setiap cabang adalah UNIK") must hard-reject duplicates — save is blocked entirely and cannot proceed; fields allowed to repeat (e.g. phone numbers) only get an informational warning and still allow saving. Confidence: 0.8
- Wants visible loading feedback on action buttons during async operations (e.g. the "Cek Dulu" button turns into a spinner + "Mengecek…" label and is disabled while the AJAX request is in flight, then restored on success/error) so it's clear a process is running. Confidence: 0.7
- Treats the alternate/secondary customer phone number (`nomor_pelanggan_2`) as first-class: it must be matched in identity lookups (`is_pelanggan`), included in SELECTs for downstream consumers, and used as a send-target fallback when the primary number is empty — across ALL WhatsApp/notification/delivery paths, so it "berfungsi secara maksimal". Confidence: 0.8
- Prefers production incidents to be investigated directly on the VPS via SSH (using the `ai-agent` alias with the SSH user the user provisioned for the assistant), cross-referencing live production logs (e.g. `wa_autoreply.log`, `wa_error_autoreply.log`) against the reported symptoms to pin down the root cause — not just analyzing the local copy of the code. Confidence: 0.8
- When diagnosing a production error from a third-party dependency (e.g. Baileys/libsignal `MessageCounterError: Key used already or never filled` during WhatsApp message decryption), first classifies it as library-internal vs. project code — reads the stack trace's module path (`node_modules/libsignal/...`) to confirm it's not from the project's own code before proposing any fix. Confidence: 0.7
- Judges production errors by fatality: a decrypt/session error that is followed by a successful reconnect (e.g. `WhatsApp connected`) is non-fatal and safe to ignore unless it recurs at high frequency ("kalau cuma sesekali di log, aman diabaikan"); only frequent errors or messages actually failing to process warrant action (e.g. a one-time service restart to reset session state). Confidence: 0.75
- Suspects duplicate processes sharing the same auth/session folder as a cause of session-state conflicts (e.g. two `node server.js` processes both writing a WhatsApp `auth/` folder → decrypt failures), and verifies on the VPS by checking which process actually holds the listening port (`ss -tlnp`) and each process's cmdline/cwd — not just assuming from `ps` output that many `node server.js` rows mean duplicates. Confidence: 0.7
- When running multi-line bash with variables/loops over SSH from a Windows local shell, writes the script to a local scratchpad file and `scp`s it to `/tmp` on the VPS then executes it (`bash /tmp/script.sh`) — inline quoted ssh commands get mangled by the Windows/cmd quoting layer and silently misbehave (e.g. `$p` not expanded), while the scp'd script runs correctly. Confidence: 0.75
- Prefers deploying changes to the VPS-hosted `mdl` project via git: commit and push from the local repo (GitHub `origin/main`), then `git pull` on the VPS — instead of editing/patching files directly on the VPS (which the read-only SSH user cannot write anyway). Explicitly forbids ANY CRUD performed directly on the VPS without a preceding pull — all VPS changes must flow through git ("jangan pernah melakukan crud di vps saya, tanpa pull"). Confidence: 1.0
- Makes an explicit exception to the git-pull-only rule for files that are in `.gitignore` (e.g. `app/Config/URL.php`, per-server local config): those may be edited directly on the VPS in place ("anda edit saja semua const yang diperlukan, selagi termasuk git ignore, Anda saya izinkan ubah") — the strict git flow applies to tracked code, while untracked/local-only config can be patched directly on the server. Confidence: 0.85
- After the user changes VPS permissions/access (e.g. adding the SSH user to the `www` group), expects the assistant to disconnect and reconnect the SSH session before retrying, since new group memberships only take effect in a fresh session ("silahkan diskonek ssh dan konek ulang dulu"). Confidence: 0.7
- After a successful git pull/deploy to the VPS, expects the assistant to verify the deployment on the server itself before declaring it done: grep for the new code/markers in the pulled files and run `php -l` lint on the changed PHP files — and for bug fixes, functionally exercise the previously-failing code path on the VPS (e.g. scp a small PHP CLI smoke test to /tmp that requires `init.php` + the failing helper and instantiates it, then run it with `php`) to confirm the error is actually gone, not just syntactically valid. Confidence: 0.85
- When handing over production DB commands for the user to run on the VPS, provides a safe variant to avoid locking large tables (`ALGORITHM=INPLACE, LOCK=NONE` for InnoDB `ADD INDEX`), notes execution risks (table locks on big tables, run during quiet hours), and includes verification queries (`SHOW INDEX ...`) so the user can confirm the change applied. Confidence: 0.6
- Handles root-level VPS operations themselves from their own root terminal/aaPanel, with the assistant only diagnosing and providing the exact commands — spanning filesystem permission fixes (chown/chmod on the git working tree and `.git`) AND production DB DDL (e.g. adding composite indexes to large tables: "berikan solusi 1, berikan command nya saja, biar saya yang jalan kan di vps"). The assistant should not attempt sudo/escalation or run privileged commands on the VPS; hand over the commands (with safe variants like `ALGORITHM=INPLACE, LOCK=NONE` and verification queries) and only verify/retry once the user confirms they've run them. Confidence: 0.9
- Expects standing workflow rules (e.g. the VPS git-pull deployment flow) to persist across sessions and terminal switches — the user believes such rules are stored permanently and should keep applying in every future session, not just the conversation where they were stated. Confidence: 0.8
- Explicitly confirms ("catat") when the assistant proposes recording a significant setup/deployment workflow into the durable project taste/preferences file — the user wants important infra/config details (remotes, bare repo paths, deploy hook behavior) persisted for future cross-session reference rather than left only in the conversation. Confidence: 0.55
- Does NOT want the assistant to automatically SSH into the VPS at the start of every session or task; SSH to `ai-agent` (194.233.94.47) should be used ONLY when the user explicitly asks for log checks or troubleshooting (e.g. checking the WA auto-reply, or "boleh bantu cek vps saya, mengapa ... tidak bisa di akses"). For normal work, stay in the local repo; production changes still flow through push → pull. Confidence: 0.95
- VPS login config for the mdl project (user asked to keep this saved): SSH host alias `ai-agent` (HostName 194.233.94.47), user `ai-agent`, IdentityFile `~/.ssh/cursor_readonly` (this key is still the one the VPS accepts for the `ai-agent` user — verified by testing when the alias was renamed from `vps-cursor`/`cursor-readonly`; `id_ed25519` was rejected); project at `/www/wwwroot/mdl`; git remote GitHub `loehur/mdl`, branch `main`; working tree owned by www:www with the SSH user in group www, so production changes only via git pull. Confidence: 0.95
- Deployment workflow for mdl now uses TWO remotes that must both be kept (user: "Beritahu saya jika sudah selesai" for this migration): `origin` = GitHub `loehur/mdl` (backup/history/clone across laptops, NEVER removed) and `vps` = `ai-agent@ai-agent:/home/ai-agent/git/mdl.git` (direct deploy). The `vps` URL intentionally uses host alias `ai-agent` (NOT the raw IP 194.233.94.47) because only then does the SSH config select the `cursor_readonly` IdentityFile — `ai-agent@194.233.94.47` falls back to the default key and fails with `Permission denied (publickey)`. Daily flow: commit → `git push vps main` (auto-deploy) → at end of work `git push origin main` (backup). The VPS has a bare repo at `/home/ai-agent/git/mdl.git` (owner ai-agent, default branch main) with a `post-receive` hook that deploys to `/www/wwwroot/mdl` via fast-forward-only (`fetch + merge --ff-only`); if the VPS working tree has local changes that conflict, the deploy is REJECTED without overwriting anything (chosen "pull aman" policy) — so keep pushed files clean of local edits, and the hook intentionally leaves local-only changes (deleted package-lock, modified data files, untracked uploads/.well-known) untouched. All runtime config e.g. `.env`, uploads, and untracked files stay out of commit scope. Confidence: 0.9
- When the user says a feature is "khusus Admin" (admin-only), expects role-based access control enforced server-side (write/change endpoints reject non-admin roles, e.g. 403), not just hidden in the frontend UI — non-admin roles (crew/driver) must still be able to read/view the data. Sensitive financial actions specifically (e.g. setting/giving discounts in Penjualan) must be gated behind the app's admin Access Key (4-digit): only an admin user (privilege 100) whose stored access_key matches the submitted key may perform them, with the check done server-side (wrong key / non-admin → rejected) — not merely hidden in the frontend. Confidence: 0.85
- Prefers sending raw error output as troubleshooting evidence — pasting exact copy-pasted console text (browser console: failing URL/status and full stack trace, e.g. `POST https://api.nalju.com/CRM/Auth/login 500 (Internal Server Error)`, or `installHook.js:1 [AutoReload] App did not initialize...` with stack lines) AND full production server log lines (e.g. a complete pino JSON log entry from fonnte_server with level/time/pid/hostname and `err.stack`, asked as "ini error apa ya"), even proactively when unsure whether the message is relevant — so the assistant can identify the exact failing endpoint/library (e.g. libsignal `MessageCounterError`) instead of guessing. Confidence: 0.85
- Prefers shared domain logic that spans multiple apps (e.g. customer/pelanggan handling used by both CRM and Laundry) to be centralized in the backend API as a single source of truth ("agar crm dan laundry dapat terpusat menggunakannya"), rather than each app carrying its own duplicated copy of the logic. Confidence: 0.8
- When centralizing business logic into the backend API, prefers limiting the scope to the shared CRUD/write operations while leaving read-only access paths (dropdowns, session-based lists, other controllers' direct reads) on direct DB access — a pragmatic, lower-risk scoping rather than rewriting every read path. Confidence: 0.6
- When services (Laundry and the centralized API) run on the same VPS, prefers internal server-to-server calls to hit the API via loopback (localhost/127.0.0.1) instead of the public domain (api.nalju.com) to skip DNS + TLS overhead for speed ("apakah bisa di akali supaya langsung panggil localhost... agar lebih cepat, soalnya masih dalam satu vps"); implemented via a configurable base URL (env `API_BASE_URL` / local config const with default fallback) rather than per-helper hardcoded literals. Confidence: 0.7
- When setting up an internal loopback service on the VPS, prefers a unique/uncommon port (e.g. `127.0.0.1:8832`) over widely-used defaults like 8080/8000, to keep it separate from common tooling and avoid collisions. Confidence: 0.5
- Switching the API base URL to loopback must NOT break browser-facing content: media URLs embedded in UI (e.g. chat bubble images) still have to resolve via the public domain (api.nalju.com) because the browser cannot reach 127.0.0.1 — the internal loopback base URL used for server-to-server calls must be kept separate from the public-facing base URL used to build media/content links. Confidence: 0.65
- The project's legacy PHP framework (Laundry `app/Core` + `app/Helper`) has no real autoloading — `spl_autoload_register` in `app/init.php` covers only `Core/` and `Models/` classes, while helper classes (e.g. `ApiLoopback`) are only loaded via explicit `require_once` in `Controller::helper()`, so a helper constructing another helper in its `__construct()` fails with `Class not found` unless loaded first. Expects the fix to be at the framework level (extend the autoloader to also load `Helper/{class}.php`) so all helpers load automatically, rather than patching individual helper constructors. Confidence: 0.8
- Wants the CRM Customer Panel's NOMOR card to show the customer's actual chat number (from `conversation.wa_number`) as a distinct "C" badge row ONLY when no stored primary/secondary numbers exist; when P/S data is present, the C row must be hidden to avoid redundant display of the same number ("jika ada data nomor P, nomor C tidak perlu tampil"), and instead a small "C" badge is added to the P or S row whose number matches the active chat number — every displayed phone row keeps its own copy-to-clipboard button with per-row "Copied!" feedback. Confidence: 0.75
- When a UI panel mixes always-available data with optional/related data (e.g. chat number C vs. laundry-linked P/S numbers), expects the always-available data to stay visible even when the related data is missing — don't hide the whole panel behind one `v-if`; render the available rows and downgrade the "belum terhubung" notice to a small note instead of replacing the card. Confidence: 0.75
- When reporting a bug, scopes the symptom precisely (e.g. old chat images still display, only newly-received ones break) and explicitly leaves the cause open ("ntah karena itu atau karena yang lain"), expecting the assistant to verify against live data (DB media_url values, actual files on disk, folder permissions) instead of taking the user's own hypothesis (e.g. the loopback base-URL change) at face value — the real cause may be elsewhere (here it was uploads folder permissions, not the base URL). Confidence: 0.6
- Treats page-load performance and app-openability as critical: reports when the CRM "tidak bisa dibuka" (cannot be opened) and is very slow ("lemooot sekali"), with the URL stuck repeatedly gaining `?_retry=1&_t=...` params — and expects the reload-loop/failure cause to be diagnosed and fixed rather than dismissed as harmless dev-tooling noise (e.g. an Expo `installHook.js` AutoReload message the assistant suggested ignoring). Being able to open the page eventually does NOT resolve the complaint: the user still flags the remaining slowness ("memang akhirnya bisa, tapi jadi lemooot sekali") as an outstanding problem to be fixed. Confidence: 0.85
- The CRM frontend the user opens is at `https://cms.nalju.com` (the user referred to it as "crm" but pasted this URL when reporting it would not load and kept getting `?_retry=1&_t=...` suffixes). Confidence: 0.5
- Prefers primary action buttons (e.g. the "Selesai" complete button) placed at the bottom-right corner of a card ("di pojok kanan bawah"), with the secondary action (e.g. Edit) on the left — the action row laid out as `justify-between` with the primary action right-aligned. Confidence: 0.55
- For optional/empty data in the UI (e.g. an alternate phone number that hasn't been set), prefers NOT showing placeholder text like "S Belum ada" — the empty row should be hidden/omitted entirely (render nothing for that row) rather than displayed with a "not yet available" label; the always-available rows still stay visible. Confidence: 0.7
- When the user frames a request as analysis-only ("saya hanya tanya, tidak perlu lakukan perubahan, hanya analisa saja" — just asking, no changes, only analyze), expects the assistant to respect that boundary strictly: investigate the actual code/data flow and report the root cause (confirming or disproving the user's hypothesis) without editing files, building, or deploying. Confidence: 0.9
- When the user reports performance issues (e.g. a page "agak lambat" or "/Rekap/i lumayan terasa lambat") — whether or not they offer a hypothesis about the cause — expects the assistant to verify against the actual code/data flow — tracing whether the suspect code path is even invoked for that page — rather than accepting the hypothesis; performance analysis focuses on likely culprits like non-sargable queries (`DATE(NOW())`, `DATE(insertTime)`, `DATE_FORMAT(insertTime,'%Y-%m')` in WHERE disabling indexes, full scans, large `IN` lists). Confidence: 0.8
- When the user reports a slow page and asks for a solution ("mungkin ada solusi juga biar bisa lebih cepat"), it is an implementation request, not analysis-only: expects the assistant to actually fix it — measure the page's real query timings on the live DB via a scp'd PHP timing script, rewrite the offending non-sargable date filters (`DATE(...)`/`DATE_FORMAT(...)` → sargable `insertTime >= ... AND insertTime < ...` range) in the code, deploy through git push/pull, and provide the index DDL (with `ALGORITHM=INPLACE, LOCK=NONE`) for the user to run as root — then re-measure after the indexes are applied. Confidence: 0.65
- During analysis-only requests, explicitly approves running read-only diagnostics directly against the production VPS/DB to confirm the root cause (user: "coba cek"): e.g. scp a small PHP CLI script to `/tmp` that times the page's actual SQL queries (per-query ms, rows) and inspects table indexes on the live DB, then reports measured numbers and proposes fixes (e.g. composite indexes) without executing any change. Confidence: 0.6
- When adding a related feature/action, prefers it placed inside an existing modal rather than creating a new separate one (e.g. adding "Ubah diskon" for non-members into the existing "Ubah ke Member" modal — "di modal itu juga") — consolidate related actions into the same dialog. Confidence: 0.5
- When the user requests a UI terminology change (e.g. "Penarikan" → "Setoran" in Kas: "gitu penarikan ubah jadi Setoran"), the rename should be applied comprehensively to every user-visible surface — menu/dropdown items, modal titles, field labels, buttons, hint/validation messages, history row labels, and stored note/report values (e.g. `note_primary`) — while internal code identifiers (method names, CSS/JS classes, variables) may stay unchanged to avoid breaking behavior. Confidence: 0.6
- For slow pages (e.g. Rekap), prefers a lazy-load/AJAX pattern to make the page "terlihat cepat": the page shell plus date/period filters render immediately on open, and the data is fetched via AJAX afterwards and shown with a loading indicator ("Memuat data rekap…") — filter changes also reload data via AJAX without a full page reload. When offered a choice, picked the full AJAX approach (data fetched separately, real perceived-speed gain) over a cosmetic skeleton overlay that just hides server-side render time. Confidence: 0.8
- Wants mobile/small-screen navigation behavior to differ from desktop: on phones/small screens (≤992px), tapping a sidebar/offcanvas menu item with a real navigation href should immediately auto-hide (close) the menu so the target page is visible — "jika menu diklik/tap, langsung hide saja" — while on desktop the sidebar must stay open (no auto-close). Parent dropdown items (href '#') must keep toggling the accordion without closing the menu. Confidence: 0.8
- Wants clicking a navigation menu item (nav-link with a real href) to show a fullscreen loading overlay: screen darkened/blurred with a centered spinner, blocking all clicks until the new page finishes loading — and this must apply on ALL screen sizes including desktop, not just mobile; the overlay hides automatically on the new page's `window.load` with a fallback timeout in case the load fails or hangs. Confidence: 0.8
- Uses the fastest comparable page in the app as a performance benchmark for others: asks for a page's first load to be made as fast as a reference page ("biar Load pertama bisa cepet juga seperti di Antrian") — expects load-speed parity across pages of similar functionality, not just marginal improvement. Confidence: 0.6
- Wants data tables to stretch to full screen width ("tabel data ... full layar width, jangan di batasi maxwidth nya") — not constrained by content-sized containers like `col-auto` or max-width; the fix is to use full-width columns (e.g. `col-12`) and this should be applied consistently across all sibling views of the same page family (e.g. all three SetHarga views: all, diskon, diskon_khusus). Confidence: 0.7
- In completion/action flows (e.g. the offcanvas shown when finishing an Antar delivery), when a field's value is already recorded in the system (e.g. the "pengisi surcas" assignee already exists on the nota), prefers to HIDE that input field entirely ("jangan tampilkan lagi input pengisi surcas Antar") rather than show it as a locked/disabled control — replace it with a small read-only info line showing the recorded value ("Sudah ada di nota — {nama}"). Confidence: 0.7
- Treats certain fields as immutable once their parent record is created ("surcas jika sudah dibuat, pengisi tidak akan pernah bisa di isi lagi, atau di ubah"): completion flows must NEVER ask to fill/change such a field again — even when the stored value is empty. The trigger for locking the field is the parent record existing (surcas sudah ada/jumlah terisi), not the field value being set. A surcas created via the customer portal (portal J, id_user=0) implicitly has the customer as its pengisi, so the input is hidden and replaced with info like "Surcas dari customer (portal J) — pengisi tidak bisa diubah" instead of being shown for someone to fill. Confidence: 0.9
- Form validation must stay consistent with field lock/visibility state: when an input is hidden because its value is already recorded/immutable (e.g. pengisi surcas already exists on the nota), the submit validation must skip the "wajib" check for that field too — otherwise the user gets a blocking toast ("Wajib pilih pengisi surcas") even though the field is deliberately hidden; the value should also not be re-sent to the backend so existing DB data is left untouched. Confidence: 0.7
- After a change is deployed, wants to know explicitly whether a service restart is required to activate it ("perlu restart fonnte server?") — expects the assistant to distinguish long-running services (Node: new code only takes effect after restart) from per-request-loaded code (PHP: active immediately, no restart needed) and state which applies to the change at hand, rather than leaving the activation step implicit. Confidence: 0.55
- Standing WhatsApp-report rule — REPLACES the former git-pull-only rule (the user said "git pull tidak lagi perlu"): whenever the user says "beritahu saya ketika selesai" (or similar "tell me when done"), the assistant must send a WhatsApp message to the group "MDL - AI Report" (`120363427412459502@g.us`) every time a task completes, for the REST of the current terminal session, until the user says stop/berhenti ("sampai saya mengatakan stop/berhenti beritahu saya"). During the work, if the assistant needs the user to answer something mid-task, it must ALSO message the group explaining that there's something the user needs to answer regarding the task ("ada hal yang perlu saya jawab mengenai ..."), not just block on the CLI. Send via the fonnte_server send endpoint — `POST https://fonnte.nalju.com/send` with header `Authorization: NeliArnisLuhurJaggu` (the FONNTE_TOKEN value) and body `{target: "120363427412459502@g.us", message: "..."}`, keeping messages short/concise ("singkat padat"). Confidence: 0.95
- During long-running/autonomous work sessions, any question that genuinely needs the user's answer must be delivered to the WhatsApp group "MDL - AI Report" (`120363427412459502@g.us`) via the fonnte send endpoint rather than only asked in the terminal/chat and blocked on — the user may not be watching the CLI while the assistant works, so questions are routed to where the user will actually see them ("di sepanjang anda bekerja, ingin bertanya yang butuh saya jawab, beritahu saya ke group fonnte tadi ya"). The assistant acknowledged this with "Mengerti". Confidence: 0.85
- When a new automation/integration is set up (e.g. the git-pull → WhatsApp group report rule), wants it actually tested end-to-end with a real message before relying on it ("coba test kirim pesan kesitu, saya ingin tahu berhasil atau tidak") — the setup is not trusted until the send succeeds and the outcome is reported back. Confidence: 0.6
- Wants to switch the mdl project's deployment workflow to a push-to-deploy model: instead of the earlier GitHub-push → VPS-git-pull flow, prefers pushing directly to a new `vps` Git remote (bare repo at a path like `/home/ai-agent/git/mdl.git`) so `git push vps main` auto-deploys to production `/www/wwwroot/mdl` via a post-receive hook — with `origin`/GitHub retained as a SEPARATE remote serving only backup, history, clone-from-other-laptop, and cross-machine sync (never to replace the VPS remote, never delete `origin` or history). Development commits push to `vps`; backup commits push to `origin`. Confidence: 0.8
- When setting up any deployment/automation change with production impact, prefers a staged approach with verification after each important step before proceeding, and specifically wants the assistant to STOP and ask for confirmation rather than guess or take risky action when the situation is ambiguous or could damage the production repository (e.g. structure mismatch, risk of losing files/history, or running a deployment hook unverified) — "jangan menebak; berhenti dan minta konfirmasi". Confidence: 0.8
- For the push-to-deploy post-receive hook, chooses a SAFE fast-forward-only deploy (options: "Pull aman" over "Reset hard + clean"): the hook runs `fetch` + `merge --ff-only` into `/www/wwwroot/mdl` so that if the VPS working tree has local changes that conflict with the incoming commit, the deploy is REJECTED rather than overwriting anything; local production working-tree changes (deleted/modified/untracked files like package-lock, `lid_phone_map.json`, uploads) are never clobbered or force-restored — only files actually changed by the pushed commit are updated, and lock files are treated as belonging to git (deploy consistent versions). Confidence: 0.75
- Explicitly forbids the assistant from EVER running heavy/queries on the VPS SQL database ("saya tidak izinkan Anda melakukan query yang berat di sql vps saya, jangan pernah") — even though root-like VPS access has been granted — because the tables hold large amounts of data ("data yang banyak") and a heavy query could hang/stack the SQL server; any DB work on the VPS must be lightweight and safe. Confidence: 0.9
- When removing a field/feature from the app (e.g. "pengisi surcas"), prefers a strict order: first remove it from the code entirely so the field is no longer required/visible anywhere (creating a surcas must not ask for pengisi anymore), and only after that code change succeeds may the DB query to drop the field be run — never drop/alter the DB column while code still references it. Confidence: 0.75
